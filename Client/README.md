# Client

Unity 客户端工程目录。

当前工程位置：

```text
Client/UnityProject/
```

当前 Unity 版本：

```text
6000.3.20f1
```

当前状态：

- Unity URP 初始工程已创建。
- Input System 已在 `Packages/manifest.json` 中启用。
- 项目自己的 Boot、Network、UI、Resource、Gameplay、Combat、Sync 和 Debug 模块尚未实现。

命名约定：

- 目录名使用稳定工程路径 `UnityProject`，避免空格路径影响脚本和命令行工具。
- Unity 产品名仍保留为 `Online Action RPG Tech Demo`，用于编辑器和后续 Build 展示。

